import fs from"node:fs";import{describe,expect,it}from"vitest";describe("invitation templates",()=>{for(const file of["app/people/actions.ts","app/people/manager-actions.ts"]){it(`${file} retains dynamic values`,()=>{const source=fs.readFileSync(file,"utf8");expect(source).toContain("${name}");expect(source).toContain("${email}");expect(source).toContain("${password}");expect(source).toContain("process.env.APP_URL")})}});
