import{describe,expect,it}from"vitest";import{french}from"@/lib/translations";describe("French catalogue",()=>{it.each([["Projects","Projets"],["Reports","Rapports"],["Password","Mot de passe"],["Publish to client","Publier au client"],["Blocked","Bloqué"]])("translates %s",(source,target)=>expect(french[source]).toBe(target));it("contains no empty translations",()=>expect(Object.values(french).every(Boolean)).toBe(true))});
