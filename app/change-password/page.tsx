import{redirect}from"next/navigation";import{getSession}from"@/lib/auth";import{PasswordForm}from"./form";export default async function Page(){if(!await getSession())redirect("/login");return <main className="auth-page"><section className="auth-card"><img src="/Logo%20DS%20colored.png" alt="How's DS"/><p className="eyebrow">ACCOUNT SECURITY</p><h1>Choose a new password</h1><p>Replace your temporary password before continuing.</p><PasswordForm/></section></main>}
