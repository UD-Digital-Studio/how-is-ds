"use client";import{useActionState}from"react";import{changePassword}from"./actions";import{PasswordInput}from"@/components/password-input";export function PasswordForm(){const[state,action,pending]=useActionState(changePassword,{});return <form action={action} className="login-form"><PasswordInput name="password" label="New password" minLength={10}/><PasswordInput name="confirm" label="Confirm password" minLength={10}/>{state.error&&<p className="form-error">{state.error}</p>}<button className="primary" disabled={pending}>{pending?"Saving…":"Set new password"}</button></form>}
