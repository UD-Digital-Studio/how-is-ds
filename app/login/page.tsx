import Link from"next/link";import {redirect} from "next/navigation";import {getSession} from "@/lib/auth";import {LoginForm} from "./login-form";
export default async function LoginPage(){if(await getSession())redirect("/");return <main className="auth-page"><section className="auth-card"><img src="/logo-ds.png" alt="How's DS"/><p className="eyebrow">PROJECT FOLLOW-UP</p><h1>Welcome back</h1><p>Sign in to see the projects assigned to you.</p><LoginForm/><Link className="auth-link" href="/forgot-password">Forgot password?</Link></section></main>}
