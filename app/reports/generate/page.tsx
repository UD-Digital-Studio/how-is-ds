import{redirect}from"next/navigation";import{getSession}from"@/lib/auth";import{db}from"@/lib/db";import{generateDraft}from"./actions";export default async function Generate(){const s=await getSession();if(!s)redirect("/login");const p=await db.query("select p.id,p.name from projects p join project_members pm on pm.project_id=p.id where pm.user_id=$1 and pm.role in ('OWNER','MANAGER') order by p.name",[s.userId]);if(!p.rowCount)redirect("/");return <main className="auth-page"><section className="auth-card"><p className="eyebrow">SMART DRAFT</p><h1>Generate report</h1><p>Build a draft from live progress, completed tasks, blockers and meeting updates.</p><form action={generateDraft} className="login-form"><label>Project<select name="project">{p.rows.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><button className="primary">Generate editable draft</button></form></section></main>}
