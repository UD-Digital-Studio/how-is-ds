import{redirect}from"next/navigation";import{getSession}from"@/lib/auth";import{db}from"@/lib/db";import{createReport}from"./actions";
export default async function NewReport(){const s=await getSession();if(!s)redirect("/login");const p=await db.query("select p.id,p.name from project_members pm join projects p on p.id=pm.project_id where pm.user_id=$1 and pm.role in ('OWNER','MANAGER')",[s.userId]);if(!p.rowCount)redirect("/");return <main className="auth-page"><section className="auth-card update-card"><p className="eyebrow">CLIENT REPORT</p><h1>Create report</h1><form action={createReport} className="login-form"><label>Project<select name="project">{p.rows.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></label><label>Title<input name="title" required/></label><label>Report<textarea name="body" className="report-body" required placeholder="Summary, progress, blockers and next steps…"/></label><button className="primary">Save draft</button></form></section></main>}
