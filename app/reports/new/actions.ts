"use server";import{redirect}from"next/navigation";import{db}from"@/lib/db";import{getSession}from"@/lib/auth";
export async function createReport(data:FormData){const s=await getSession();if(!s)redirect("/login");const project=String(data.get("project"));const a=await db.query("select 1 from project_members where project_id=$1 and user_id=$2 and role in ('OWNER','MANAGER')",[project,s.userId]);if(!a.rowCount)throw new Error("Not authorized");const r=await db.query("insert into reports(project_id,title,body,created_by) values($1,$2,$3,$4) returning id",[project,String(data.get("title")).trim(),String(data.get("body")).trim(),s.userId]);redirect(`/reports/${r.rows[0].id}`)}
