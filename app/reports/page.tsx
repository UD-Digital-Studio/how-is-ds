import Link from"next/link";import{redirect}from"next/navigation";import{db}from"@/lib/db";import{getSession}from"@/lib/auth";
export default async function Reports(){const s=await getSession();if(!s)redirect("/login");const r=await db.query(`select r.id,r.title,r.status,r.created_at,r.published_at,p.name project_name,pm.role,count(nd.id)::int deliveries,count(nd.id) filter(where nd.status='FAILED')::int failed from project_members pm join projects p on p.id=pm.project_id join reports r on r.project_id=p.id left join notification_deliveries nd on nd.report_id=r.id where pm.user_id=$1 and (pm.role<>'CLIENT' or r.status='PUBLISHED') group by r.id,p.name,pm.role order by r.created_at desc`,[s.userId]);const canCreate=r.rows.some(x=>x.role!=="CLIENT");return <main className="project-page"><div className="project-top"><Link href="/">← Dashboard</Link>{canCreate&&<div><Link href="/reports/generate">Generate report</Link><Link href="/reports/new" className="primary">＋ Manual report</Link></div>}</div><section className="project-hero"><p className="eyebrow">CLIENT COMMUNICATION</p><h1>Reports</h1><strong>{r.rowCount} visible report{r.rowCount===1?"":"s"}</strong></section><section className="report-list">{r.rows.length?r.rows.map(x=><Link href={`/reports/${x.id}`} key={x.id}><div><span>{x.project_name}</span><strong>{x.title}</strong><small>{x.published_at?new Date(x.published_at).toLocaleDateString():"Not published"}</small></div><em className={x.status.toLowerCase()}>{x.status}</em>{x.role!=="CLIENT"&&<small>{x.deliveries?`${x.deliveries-x.failed} sent${x.failed?`, ${x.failed} failed`:""}`:"No delivery yet"}</small>}<b>›</b></Link>):<div className="empty-state">No reports yet.</div>}</section></main>}
