"use client";import{useActionState}from"react";import{sendTest}from"./actions";export function TestForm({projects,clients}:{projects:{id:string;name:string}[];clients:{id:string;full_name:string;projects:string[]}[]}){const[state,action,pending]=useActionState(sendTest,{});return <form action={action} className="login-form"><label>Project<select name="project">{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label>Client<select name="recipient">{clients.map(c=><option key={c.id} value={c.id}>{c.full_name}</option>)}</select></label><label>Channel<select name="channel"><option value="email">Email</option><option value="whatsapp">WhatsApp</option></select></label><p className="field-help">The selected client must belong to the selected project.</p>{state.error&&<p className="form-error">{state.error}</p>}{state.ok&&<p className="import-ok">✓ {state.ok}</p>}<button className="primary" disabled={pending}>{pending?"Sending…":"Send test notification"}</button></form>}
