"use client";import{useActionState}from"react";import{requestReset}from"./actions";export function ForgotForm(){const[state,action,pending]=useActionState(requestReset,{});if(state.ok)return <p className="import-ok">If the account exists, a reset link has been sent.</p>;return <form action={action} className="login-form"><label>Email<input name="email" type="email" required/></label><button className="primary" disabled={pending}>{pending?"Sending…":"Send reset link"}</button></form>}
