"use client";import{useActionState}from"react";import{addClient}from"./actions";export function ClientForm({projects}:{projects:{id:string;name:string}[]}){const[state,action,pending]=useActionState(addClient,{});return <form action={action} className="login-form"><fieldset className="project-checks"><legend>Projects</legend>{projects.map(p=><label className="check-row" key={p.id}><input name="projects" type="checkbox" value={p.id}/>{p.name}</label>)}</fieldset><label>Client name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>WhatsApp number<input name="phone" type="tel" placeholder="+2376XXXXXXXX"/></label><label>Preferred language<select name="locale"><option value="en">English</option><option value="fr">Français</option></select></label><label className="check-row"><input name="opt_in" type="checkbox"/> Client agrees to receive WhatsApp updates</label>{state.error&&<p className="form-error">{state.error}</p>}{state.ok&&<p className="import-ok">✓ {state.ok}</p>}<button className="primary" disabled={pending}>{pending?"Saving…":"Save client"}</button></form>}
