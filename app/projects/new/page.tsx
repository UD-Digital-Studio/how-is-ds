import{redirect}from"next/navigation";import{db}from"@/lib/db";import{getSession}from"@/lib/auth";import{ProjectForm}from"./form";export default async function Page(){const s=await getSession();if(!s)redirect("/login");if(!(await db.query("select 1 from project_members where user_id=$1 and role='OWNER' limit 1",[s.userId])).rowCount)redirect("/");return <main className="auth-page"><section className="auth-card update-card"><p className="eyebrow">PORTFOLIO</p><h1>New project</h1><p>Create the project, then import or build its roadmap.</p><ProjectForm/></section></main>}
