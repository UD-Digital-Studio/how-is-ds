"use server";import{cookies}from"next/headers";import{revalidatePath}from"next/cache";import{db}from"@/lib/db";import{getSession}from"@/lib/auth";export async function setLocale(data:FormData){const locale=data.get("locale")==="fr"?"fr":"en",s=await getSession();(await cookies()).set("hows_ds_locale",locale,{path:"/",sameSite:"lax",secure:process.env.NODE_ENV==="production",maxAge:31536000});if(s)await db.query("update users set locale=$1 where id=$2",[locale,s.userId]);revalidatePath("/","layout")}
