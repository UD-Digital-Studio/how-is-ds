"use client";import {useActionState} from "react";import {importRoadmap} from "./actions";
export function ImportForm({projects}:{projects:{id:string;name:string}[]}){const[state,action,pending]=useActionState(importRoadmap,{});return <form action={action} className="login-form"><label>Project<select name="project" required>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label>CSV roadmap<input name="file" type="file" accept=".csv,text/csv" required/></label><p className="field-help">Dates, assignees, descriptions, weights, and statuses are optional.</p>{state.errors&&<div className="import-errors">{state.errors.map(x=><p key={x}>{x}</p>)}</div>}{state.ok&&<p className="import-ok">✓ {state.ok}</p>}<button className="primary" disabled={pending}>{pending?"Importing…":"Validate and import"}</button></form>}
