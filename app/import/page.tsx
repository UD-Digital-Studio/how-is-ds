import {redirect} from "next/navigation";import {getSession} from "@/lib/auth";import {db} from "@/lib/db";import {ImportForm} from "./import-form";
export default async function ImportPage(){const s=await getSession();if(!s)redirect("/login");const r=await db.query("select p.id,p.name from project_members pm join projects p on p.id=pm.project_id where pm.user_id=$1 and pm.role in ('OWNER','MANAGER') order by p.name",[s.userId]);if(!r.rowCount)redirect("/");return <main className="auth-page"><section className="auth-card update-card"><p className="eyebrow">PROJECT SETUP</p><h1>Import roadmap</h1><p>Populate milestones, FRs and tasks from one CSV.</p><a className="template-link" href="/templates/project-roadmap-template.csv" download>↓ Download CSV template</a><ImportForm projects={r.rows}/></section></main>}
