import fs from "node:fs/promises";import pg from "pg";const{Client}=pg;const c=new Client({connectionString:process.env.DATABASE_URL});await c.connect();try{const x=await c.query("select to_regclass('public.requirements') present");if(!x.rows[0].present)await c.query(await fs.readFile("db/002_requirements_tasks.sql","utf8"));console.log("REQUIREMENTS_SCHEMA=READY")}finally{await c.end()}
